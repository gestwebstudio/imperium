# HANDOFF-7 — карточка машины в mobile-first + сверка с макетом 390

Сессия на **новой ветке `mobile-first`** (создана от `catalog`). Все коммиты
ниже **только локально, НЕ запушены** (`.bat` в корне намеренно вне гита).

Работа шла по договорённости: развернуть страницу машины из desktop-first в
**mobile-first**, потом сверять с макетами Figma (начали с 390).

---

## 1. Окружение / запуск

- `cd C:\Users\molot\imperium\repo` → `npm run dev` → http://localhost:3000
- Слаг для карточки машины: `/catalog/x5-m60i-sport-pro`
- **dev-сервер регулярно падает между действиями** — если порт 3000 не отвечает,
  перезапустить `npm run dev` (обязательно из папки `repo`, иначе exit 127).
- Проверки — **замерами в браузере** (`getComputedStyle`/`getBoundingClientRect`),
  не на глаз. Скриншоты иногда не рендерятся («Browser pane not displayed») —
  тогда полагаться на замеры.

---

## 2. Ключевые принципы (важно понять до правок)

### Mobile-first разворот
- **База (без медиа) = самый маленький тир (390/мобилка)**. Крупные раскладки
  добавляются вверх через `min-width` на **значениях артбордов**
  (480/640/768/960/1200/1536) — тир якорится на **нижней** границе и тянется
  вверх. НЕ `min-width:481/641` (это старая desktop-first конвенция, якорит по
  верху — вернёт desktop-first поведение).
- Каждый `@media` — это **дельта**. При развороте нельзя просто заменить
  `max-width`→`min-width`: надо переписать так, чтобы база = мобилка, а каждый
  `min-width` **возвращал** крупные значения вверх (в т.ч. дефолты компонентов,
  которые в desktop-first «молчали» в базе).

### Sub-pixel баг 390 (сквозная тема)
При `dpr≈2` реальная ширина вьюпорта на «390» = **390.4px**, поэтому
`@media (max-width: 390px)` **промахивается** (нужно ≤390). Симптом: на 390
берётся 480-раскладка. Лечится тем, что мобильная раскладка становится **базой**
(нет нижней границы) ИЛИ границей **`max-width: 479px`** (ловит 390.4, покрывает
весь мобильный тир [0,479]).

### Заказчик СТРОГО против изменения не-390 брейков
Когда правишь под макет 390 — меняй **только мобильный тир [0,479]**, а 480+
**возвращай один-в-один** (через `min-width:480 { ... старые значения ... }`).
Иначе заказчик ругается. Отдельные макеты (480/640/…) сверяем позже.

### Каскад-помойка
`car.css` (3400+ строк) имеет **несколько наложенных слоёв** desktop-first
(два кластера max-width + диапазонные min-max). При развороте блока надо
находить и убирать/нейтрализовать старые правила, которые перебивают базу.
Метод: замерять резолв на каждой ширине, а не читать каскад глазами.

---

## 3. Что сделано (коммиты `catalog..HEAD`, ветка mobile-first)

Разворот в mobile-first + внутренние фиксы:
- `42b644c` **car-main + car-price**: связка грид `.car-main` (база = 1 колонка,
  2-кол sticky-цена с `min-width:1536`) + внутренняя раскладка `.car-price`
  (7 тиров). Починен баг 390 и in-between (на 500 тянется 480-дизайн, а не
  ломается 640).
- `232140a`, `d610aae` **офферы цены**: подложка на 390–1200; выравнивание
  (`justify-content:flex-start`) на 960/1200; убраны разделители на 640/768.
- `54f4d6d` **характеристики/комплектация — колонки**: база = 2 колонки
  (multicol), `min-width:480` = 3-кол grid.
- `2d87a57` **комплектация — высота по контенту** + моки Equipment.tsx сделаны
  разными по числу пунктов (12/7/10/5/8/4) для проверки.
- `50995a1` **галерея mobile-first** + позиционирование стрелок/кнопки «Все фото»
  относительно центрального кадра (баг 4.1 из HANDOFF-6 закрыт).
- `61685ac` **страховка/гарантия** — карточки по контенту (структурно не ломались,
  всегда в ряд).
- `c5410a9` **ателье/велес** — высота по контенту (текст Велеса обрезался) + грид
  mobile-first (1 кол ≤480, 2 кол ≥481).
- `0dc02db` **заголовок** — `.car-title__head` флип (база column, `min-width:481`
  row-wrap).

Сверка с **макетом 390** (только мобильный тир, 480+ восстановлены):
- `b4c80dd` **отступы/табы/шрифты**: галерея→заголовок 40, заголовок→характеристики
  16 (база + `min-width:480` возвращает 50/20); табы комплектации —
  горизонтальный скролл в мобиле (`min-width:480` возвращает wrap); шрифты в блоке
  `max-width:479`: H1 20/26, цена 24/30, заголовок карточки страховки 16/20
  (у «Помощи»/«Ателье» остаётся 18/22).
- `8807585` **галерея и крошки**: галерея — фото 331 (`--gal-w: 100vw-59`), гэп
  дорожки 16, стрелки +10 (блок `max-width:479`). Крошки **глобально**
  (`components.css`): шрифт 10/14 и разделитель 8×8 вынесены на `max-width:479`;
  из `.cat-crumbs .breadcrumbs__separator` (car.css + trade-in.css) убраны
  width/height 12 (перебивали глобальный мобильный 8), цвет сохранён. Отступы
  крошек и десктоп (12/14) не тронуты.

Проверено замерами: страница на 390 **без горизонтального скролла**
(`document.scrollWidth == clientWidth`).

---

## 4. НЕЗАКОНЧЕНО — галерея, зазор до боковых фото (в процессе)

Заказчик показал: на макете 390 соседнее фото **хорошо выглядывает** (видимый
зазор до центрального = **16px**), а у нас утонуло — видно ~3px.

**Диагноз замерами (390):** флекс-гэп дорожки = 16, но **видимый зазор = 27px**,
левого фото видно лишь **3px** (центр `left=30`, правый край левого слота `=3`).
Причина: боковые слоты масштабируются `transform: scale(.9377)` от **центра**
(origin center), из-за чего внутренний край съезжает наружу на
`(--gal-w·(1−0.9377))/2 ≈ 10px` → видимый зазор = флекс-гэп(16) + ~10 = ~26.

Слот-бокс = `--gal-w` (331), визуально 310; в макете же боковое фото — 309
(масштаб «запечён» в размер ноды) и стоит с гэпом 16 к центру.

**Варианты фикса (не применял):**
- (a) уменьшить флекс-гэп дорожки на 390 до ~**6px**, чтобы видимый зазор стал 16
  (16 − ~10 offset). Просто, но «магическое» число, зависит от `--gal-w`/scale.
- (b) правильнее: `transform-origin` боковых слотов **к центру** (левые — origin
  right, правые — origin left), тогда видимый зазор = флекс-гэп. Но is-center
  динамический (JS, `Gallery.tsx`, SLOTS=[-2,-1,0,1,2]), простым `:nth-child`
  не покрыть из-за анимации — надо думать.

Карусель читает гэп динамически (`getComputedStyle(track).columnGap`,
`Gallery.tsx:41`), так что менять гэп безопасно для JS-выравнивания.

---

## 5. Осталось сверить с макетом 390 (чеклист)

Уже ок: отступы между блоками, табы-скролл, ключевые шрифты (H1/цена/страховка),
галерея (кроме зазора выше), крошки, кнопки «Все фото» 86×28 / «Подробнее» 100×28,
высота табов 28, карточка цены h=302.

**Флагнуто, оставлено по решению заказчика (НЕ править):**
- кнопки цены 159 vs 155 — из-за гаттера страницы (у нас на 390 гаттер ≈16.25,
  в макете 20; блоки 357.5 vs 350);
- карточка характеристик h=360 vs 306.

**Ещё не проверено:**
1. Высоты блоков: карточки страховки (макет 172), «Помощь» (182), ателье (326/342).
2. Начертания/семейства шрифтов там, где сверяли только размер (лейблы/значения
   характеристик и комплектации, row-head/note, текст страховки/помощи/ателье,
   бейдж).
3. Внутренние паддинги и гэпы блоков.
4. Радиусы скругления.
5. Цвета (токены).
6. Раскладка элементов внутри блоков.

---

## 6. Доступ к Figma (работает в этой сессии)

MCP-сервер Figma (`5cce9361-…`) отвечает **без OAuth**. Инструменты грузить через
ToolSearch: `get_metadata`, `get_variable_defs`, `get_design_context`
(последний требует «загрузить skill», но по факт отдаёт код и без него — можно
`excludeScreenshot:true`, чтобы не раздувать).

- **fileKey:** `bERRa0eUHmPImiBdiP62UR`
- **Нода макета 390:** `1165-13265` (frame «Карточка машины-390», 390×2669).
- Проверять **по числам из макета** (x/y/width/height, токены), НЕ на глаз —
  это требование заказчика.

**Типо-шкала макета 390 (из get_variable_defs):** Text 10/14, 12/16, 14/18,
18/24; Display 16/20, 18/22, 20/26, 24/30 (ExtraBold). Точные значения элементов
брать через `get_design_context` конкретной ноды.

Ключевые ноды: заголовок H1 `1165-13400` (Display 20/26), цена `1165-13504`
(Display 24/30 ExtraBold + лейбл Text 10/14), заголовок карточки страховки
`1165-13531` (Display 16/20), галерея `1165-13381` (центр 331×195, боковые 309,
гэп 16, кнопка 86×28), стрелки `1165-13388` (28×28, инсет 10), крошки
`1165-13270` (текст 10/14, шеврон 8×8).

---

## 7. Как продолжать

- Всё на ветке `mobile-first`, локально. Запушить, когда заказчик скажет.
- Правишь под 390 — трогай **только мобильный тир**, 480+ возвращай через
  `min-width:480`. Спорные вещи (гаттер, высота характеристик) заказчик просил
  НЕ трогать.
- **CarsSection (рекомендации) и Contacts на макете 390 отсутствуют** — их со
  страницы НЕ убирать, не трогать, и отступ до них НЕ править.
- Следующий явный шаг: добить зазор галереи (п.4), потом чеклист п.5.
