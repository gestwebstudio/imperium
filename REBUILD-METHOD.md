# Метод рерайта вёрстки (ветка `rebuild`) — обязательный чеклист для КАЖДОГО блока

Презентационный слой пересобираем заново по макетам Figma: **mobile-first, автоскейл (fluid),
на единых брейках, с чисткой легаси**. Начинку (данные/логику/анимации/токены/примитивы) НЕ
трогаем. Полная история ребилда — `HANDOFF-REBUILD.md`.

## ГЛАВНОЕ — автоскейл (суть ребилда)

Резину надо **развернуть на mobile-first И сделать плавной**. На промежуточной ширине (напр.
414) элемент должен быть **растянутой вверх версией нижнего артборда**, а не суженной вниз
версией верхнего. Значения на 8 артбордах (390/480/640/768/960/1200/1536/1920) — это **якоря**;
МЕЖДУ якорями каждое свойство растёт **непрерывно (fluid)**, без скачка на границе брейка.
Так уже сделаны услуги/каталог/главная — страница машины обязана быть такой же.

**Граница fluid / шаги — как на trade-in** (не всё подряд fluid):
- **Fluid (плавно, `clamp`):** размеры **шрифтов**; **межблочные зазоры и `gap`**; **тянущиеся
  ширины** (ширина блока = 100% колонки/контейнера скейлится сама; ширина фото типа `--gal-w`).
- **Шаги по брейкам (дискретно, `min-width`-тиры):** `padding` и `min-height` подложек-карточек;
  высоты кнопок/строк/табов; **радиус** (`--radius-current`). Так на trade-in — повторяем.

Дискретный `font-size: 20` со скачком на брейке — **НЕПРАВИЛЬНО** (шрифт обязан быть fluid).
Дискретный `padding: 20` со скачком — **нормально** (как trade-in).

### Как задавать автоскейл

1. **Готовые fluid-токены `--fs-*`** (`styles/typography.css`, шкала 360→1920):
   `--fs-88/66/50/48/40/36/32/30/28/24/20/18/16/14` — каждый `clamp(min, calc+vw, max)`.
   Годятся, когда значение растёт одной непрерывной линией по всему диапазону.
2. **Посегментный `clamp` по якорям макета** — когда на брейках свои значения (напр. H1
   карточки 20→28→32→36→40→48). Эталон в коде: **`.t-page-title`** (`styles/typography.css:533`).
   Для сегмента `Wlow→Whigh` со значениями `Vlow→Vhigh`:
   `clamp(Vlow, calc(PXpx + VWvw), Vhigh)`, где
   `VW = (Vhigh − Vlow) / (Whigh − Wlow) × 100`,
   `PX = Vlow − Wlow × (Vhigh − Vlow) / (Whigh − Wlow)`.
   Сегменты задаём через `min-width` (база = нижний диапазон, каждый брейк переопределяет).
   Пример (390→480, 20→28): `clamp(20px, calc(-14.67px + 8.89vw), 28px)`.
   Тот же приём — `--gal-w` галереи (посегментный calc между артбордами).

## Константы

- **8 артбордов:** `390 / 480 / 640 / 768 / 960 / 1200 / 1536 / 1920`. Брейки — **min-width**
  ровно на этих числах. Никаких off-by-one (`481/641/961/1201`).
- **`--gutter`** (20/20/30/40/40/40/60/60) — боковой отступ контента. Контейнер:
  `max-width:var(--layout-max); margin-inline:auto; padding-inline:var(--gutter)`. Легаси
  `.car-wrap{padding-inline}` в `max-width`-тирах сносим — иначе `--gutter` перебивается.
- **Радиусы — `var(--radius-current)`** (12/12/14/16/22/26/30/30). Не хардкодить.
- **Шрифты/цвета — токены кита** (`--fs-*`, `--type-*`, `--color-*`), сверять `get_variable_defs`.

## Цикл работы над блоком

1. **Разобрать текущее.** Прочитать CSS+разметку, вслух коротко: что не так (чужие брейки,
   резина в обратную сторону, дискретные шаги вместо fluid, мёртвые правила).
2. **Снять якоря из Figma** (fileKey `bERRa0eUHmPImiBdiP62UR`): `get_metadata` (x/y/w/h),
   `get_variable_defs` (токены), `get_screenshot`. На всех 8 артбордах.
3. **Собрать заново mobile-first + автоскейл.** База = 390, крупные значения возвращаются
   вверх через `min-width` на артбордах. Каждое свойство — **fluid между якорями** (см. выше),
   НЕ дискретный шаг. Старый desktop-first `max-width`-каскад блока — **снести**.
4. **Механику не переписываем** (JS/анимации/лайтбоксы переносим на новую разметку).
5. **Проверка ЗАМЕРАМИ** (Browser: `resize_window` + `javascript_tool` →
   `getComputedStyle`/`getBoundingClientRect`) на 8 артбордах И между ними — **обязательно
   414/500/700** (там ловится «зашагал вместо fluid»). `scrollWidth <= clientWidth`.
6. **Показать заказчику** замеры, спросить спорное. Блок доводим ЦЕЛИКОМ → коммит.

## Что НЕ трогаем на странице машины

- Блоки **«рекомендованные авто»** (`CarsSection`) и **«контакты»** (`Contacts`) —
  переподключаются отдельно, в макетах их нет. Не удалять, не верстать.
- Общие компоненты (`Badge/Button/Crumbs/Wishlist/Comparison/PriceBlock`) — берём как есть,
  правим раскладку/обёртку блока (специфичность override — `.car-title .car-title__badge`).

## Ноды Figma — карточка машины (артборд → node)

`390 → 1165:13265` · `480 → 991:3149` · `640 → 971:4908` · `768 → 959:4313` ·
`960 → 932:6218` · `1200 → 932:4966` · `1536 → 932:3874`. (1920 — уже свёрстан.)
Внутри артборда: галерея = `Frame 1000005916`; шапка-заголовок = `Frame 1000006003`;
характеристики/комплектация/цена = `Frame 1000005998`. **1200 — десктоп-раскладка** (как 1536).

## Запуск / проверка

`cd repo → npm run dev → http://localhost:3000`. Машина: `/catalog/x5-m60i-sport-pro`.
