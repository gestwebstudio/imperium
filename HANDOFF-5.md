# HANDOFF-5 — передача контекста (Imperium Motors)

Продолжение [HANDOFF.md](HANDOFF.md) / [HANDOFF-2.md](HANDOFF-2.md) /
[HANDOFF-3.md](HANDOFF-3.md) / [HANDOFF-4.md](HANDOFF-4.md) / [HANDOFF-FEED.md](HANDOFF-FEED.md).
Дата: 2026-08-08. Ветка: **`catalog`** (в неё коммитим, `origin/catalog` синхронна, всё запушено).

Проект: сайт автосалона на **Next.js (App Router)**, кит + HeroUI, данные — моки.
Отвечать по-русски, UI-тесты не гонять, коммитить после каждой задачи.

> Рабочая папка проекта — **`C:\Users\User\imperium`** (это отдельный git-репо на ветке
> `catalog`). НЕ `Desktop\imperium`. Дев-сервер: `npm run dev` (порт 3000), CSS — HMR.

---

## Главная тема сессии: адаптив по брейкам «как в макете» (дискретно, не флюид)

Заказчик рисует в Figma **отдельные адаптации под каждый брейк** и хочет пиксель-в-пиксель.
Методика (как со шапкой): **дискретные значения по брейкам**, desktop-first, база = самый
широкий тир. Проверка — **числовыми замерами в браузере** (getBoundingClientRect /
getComputedStyle), не «на глаз». Брейки-артборды (min-width): **1920 / 1536 / 1200 / 960 /
768 / 640 / 480 / 390**.

Figma-файл: `bERRa0eUHmPImiBdiP62UR`. Артборды адаптаций лежат на странице «Макеты» (9:1784).
ВАЖНО: через MCP Figma соседние артборды по одному node-id не достаются (родитель не читается),
поэтому **всегда просить у заказчика ссылки/выделение на нужные фреймы**.

## Коммиты сессии (свежие сверху)

- `81909a3` — О салоне: **VK Video** в блок видео (iframe `video_ext.php?oid=-240680187&id=456239017`).
- `734d4a2` — Футер: копирование телефона; help-on-roads: жирный текст + заголовок КТА.
- `8752e92` — О салоне: поменял местами галерею и видео.
- `80b8385` — Услуги: ряд преимуществ — сиблинг hero + единая высота карточки (фикс leasing/car-selection).
- `3fc45f4` — Шапка: боковой отступ = `--page-gutter` (край шапки = край контента).
- `64c2c6a` — Шапка: «Услуги» (nav-trigger) — вернул тир-размеры шрифта.
- `46b4f7d` — Trade-in hero+преимущества: точные адаптации по 8 брейкам.
- `ca44387` — Шапка: 1200 — десктоп-навигация без бургера + два десктоп-тира.

---

## 1. Шапка (`components/layout/layout.css`, `Header.tsx`, `ServicesMega.tsx`)

Десктоп-навигация теперь **с 1200** (было с 1201; бургер ≤1199). Три десктоп-тира по макету:

| Тир (вьюпорт) | Высота | Логотип h | Контрол (call/CTA) | Навигация |
|---|---|---|---|---|
| 1200–1535 | 66 | 20 | 40 | 14/18 |
| 1536–1919 | 66 | 26 | 40 | 16/20 |
| ≥1920 | 78 | 32 | 46 | 20/28 |

Бургерные тиры без изменений: 640–1199 → 66/лого26/бургер44; ≤480 → 58/лого20/бургер36.
- **Гаттер шапки** = `var(--page-gutter)` (padding-inline и `--header-gutter`). Раньше был
  фикс 60/40/30 → шапка была уже контента. Теперь **край шапки = край контента** везде.
  При будущем переходе на дискретный гаттер по макету — правится ОДНА переменная `--page-gutter`
  в `tokens.css`, шапка поедет за ней.
- Баг «Услуги»: базовое правило `.site-header__nav-trigger{font-size:20}` стояло НИЖЕ тир-медиа
  и перебивало их по порядку → тиры триггера продублированы ПОСЛЕ базы (низ layout.css).
- «Помощь на дороге» → **«Помощь на дорогах»** в футере И в мега-меню (крошки уже были так).

## 2. Trade-in — ПЕРВЫЙ БЛОК (hero + преимущества) — готов по 8 брейкам

Файл `app/trade-in/trade-in.css`, `components/tradein/TradeInPage.tsx`. Ушёл от `--fs-*`/clamp к
дискретным значениям. Спека (заголовок Wix Madefor reg+ExtraBold; текст Onest):

- **Заголовок**: 66/70 → 50/70 → 40/48 (1200–480) → на 390 reg 36/44 + bold 40/48.
- **Подзаголовок**: 18/24 (≥1536) → 16/20 (≤1200).
- **CTA «Экспресс-оценка»** (зелёная primary-surface): L44 (≥1200) → M36 (640–1199) → h34/14 (≤639).
- **Карточка преимущества**: value 24/30 SemiBold + label 20/28 (≥1536) → 18/24 + 16/20 (≤1200);
  **min-height карточки 116/102** (единая высота на всех услугах, значение центрируется).
- **Раскладка hero**: 2 колонки (заголовок слева + aside справа) ≥1200 → стек ≤1199 (aside снизу, слева).
- **Раскладка преимуществ**: 4-в-ряд (≥1200) → **3-в-ряд** (960–1199, 4-я «Зачёт стоимости» СКРЫТА —
  так решил заказчик) → 2×2 (640–959) → 1 колонка (≤639).
- **Наезд ряда на hero**: ряд-`ti-hero__stats-wrap` — **СИБЛИНГ после `<section.ti-hero>`** (не внутри!),
  `margin-top: -58/-51` (половина карточки). Кромка hero = ровно середина первой карточки.
  Отступ до ряда считается от более длинной колонки (заголовок/кнопка) — флексом, автоматически.

## 3. КЛЮЧЕВОЕ: блок hero+преимущества ПЕРЕИСПОЛЬЗУЕТСЯ на всех услугах

CSS общий (`trade-in.css`, классы `.ti-*`), но **JSX у каждой страницы свой**. Используют блок:
**trade-in, leasing (`LeasingPage.tsx`), car-selection (`CarSelectionPage.tsx`)** (у них `ti-hero__stats-wrap`).
help-on-roads тоже на `.ti-hero`, но без ряда преимуществ.

Грабли, которые уже чинили: у leasing/car-selection ряд был ВНУТРИ `<section.ti-hero>` → фон
растягивался / `overflow:hidden` резал карточки. Вынесли сиблингом (как в trade-in). **Правя
общий `.ti-*` CSS — проверяй ВСЕ три страницы.**

## 4. О салоне (`components/about/AboutSalonPage.tsx`, `app/about/about.css`)

- Поменяли местами: **видео — в первый блок на серую подложку** (на месте галереи, габариты
  как у галереи: ширина × height clamp(520–748)); **галерея — в отдельную секцию** ниже
  (`.about-salon__gallery-section`).
- В блок видео вставлен **VK Video** (iframe `vk.com/video_ext.php`). Видео публичное, `hash` не нужен.
  Если сделают приватным — понадобится `hash` из VK «Поделиться → Экспортировать».
- Пропорции: видео 16:9 в широкой рамке → **чёрные поля по бокам** (pillarbox). Заказчик решил
  **оставить так**. Если захочет без полей — вариант: `aspect-ratio:16/9` на `.about-salon__video`.

## 5. Футер (`components/layout/Footer.tsx`, `FooterPhone.tsx`, layout.css)

Телефон разбит: номер = `tel:`-ссылка, иконка = кнопка «Скопировать» с подсказкой «Номер
скопирован» (клиентский `FooterPhone.tsx`, логика копирования как в шапке — clipboard API + fallback).

## 6. help-on-roads (`components/helproads/HelpRoadsPage.tsx`)

- Подзаголовок hero: жирным (`<b>`) — «Круглосуточная техническая поддержка и выездной автосервис»
  и «Звонок бесплатный.».
- Заголовок нижнего КТА-баннера: «Позвоните нам» → **«Помощь 24/7»**.

---

## Открытые вопросы / что дальше

- **Trade-in: адаптированы ТОЛЬКО hero + преимущества.** Текстовые блоки («Что влияет на оценку»,
  «Мы готовы принять» — классы `.ti-factors`/`.ti-accept`/`.ti-block__title`/`.ti-callout`) по брейкам
  ЕЩЁ НЕ сделаны — ждём адаптации от заказчика. Верстать так же: дискретно, числовая сверка, и
  учитывать переиспользование на других услугах.
- Гаттер контента `--page-gutter` пока флюид `clamp(16,3.125vw,60)`; по макету дискретный 60/40/20 —
  глобальная правка (все страницы), делать только по решению заказчика.
- Видео на «О салоне» — оставлено с боковыми полями (по решению).

## Рабочие заметки

- Дев-сервер `npm run dev` :3000. Осторожно: шапка авто-прячется при скролле, есть анимация входа
  страницы (`.page-transition`) — мерить в устоявшемся состоянии, высоту шапки брать `getBoundingClientRect`.
- **Скриншоты:** ставил `playwright` через `npm i playwright --no-save` +
  `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`, гнал через **системный Chrome** (`channel:'chrome'`) — chromium
  не качал, `package.json` чистый. 8 full-page скринов услуг+О салоне (1920, 2× плотность) лежат в
  `C:\Users\User\Desktop\imperium-shots\` (01..08).
- Доки `docs/*` — по-прежнему в гит НЕ коммитим (решение заказчика).
